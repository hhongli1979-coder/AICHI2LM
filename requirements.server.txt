# Lightweight runtime dependencies for CPU/container runtime (recommended for quick start)
fastapi>=0.95.0
uvicorn[standard]>=0.18.0
httpx>=0.24.0
transformers>=4.30.0
safetensors>=0.3.1

# If you need CPU PyTorch, uncomment and adjust the wheel source as needed:
# torch==1.13.1+cpu -f https://download.pytorch.org/whl/cpu/torch_stable.html
