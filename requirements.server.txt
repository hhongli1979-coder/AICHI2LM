# requirements.server.txt - Lightweight runtime dependencies for CPU inference
# These are minimal dependencies for running TeleChat API server

# Web service frameworks
fastapi>=0.109.1
uvicorn[standard]>=0.17.6
httpx

# Model serving
transformers>=4.30.0
safetensors>=0.3.1

# For CPU-only PyTorch, you may want to install from specific wheel:
# pip install torch --index-url https://download.pytorch.org/whl/cpu
# Uncomment the line below if you want to install CPU-only torch
# torch>=1.13.1
