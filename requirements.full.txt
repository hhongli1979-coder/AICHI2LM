# TeleChat Full Dependencies
# ===============================================================================
# WARNING: This requirements file includes heavy dependencies for GPU training
# WARNING: Building with these requirements may take 30-60+ minutes
# WARNING: Some packages (flash-attn, auto-gptq) require CUDA and GPU for compilation
# WARNING: The resulting image may be 10GB+ in size
# WARNING: Recommended to use Dockerfile.full-gpu with pytorch/pytorch base image
# ===============================================================================

# Core ML frameworks
accelerate>=0.24.1
torch==1.13.1
transformers==4.30.0

# Training and optimization
deepspeed==0.8.3
peft>=0.5.0

# Quantization (requires CUDA for compilation)
auto-gptq==0.3.0

# Performance optimization (requires CUDA for compilation)
# NOTE: flash-attn may fail to build on CPU-only systems
flash-attn

# Data handling
datasets>=2.10.1
jsonlines
safetensors>=0.3.1

# API and serving
fastapi>=0.109.1
uvicorn==0.17.6

# Web interface
streamlit>=1.30.0

# Utilities
pyyaml>=6.0
psutil>=5.9.0
requests>=2.28.0
