# requirements.full.txt - Full heavy dependencies for TeleChat
# WARNING: These dependencies are VERY large (10+ GB) and require significant disk space
# WARNING: Installation can take 30+ minutes depending on your internet connection
# WARNING: Some packages (flash-attn, auto-gptq, deepspeed) require GPU and may fail on CPU-only systems

# Core ML frameworks
accelerate>=0.24.1
auto-gptq==0.3.0
deepspeed==0.8.3
datasets>=2.10.1
flash-attn
jsonlines
peft>=0.5.0
safetensors>=0.3.1
torch==1.13.1
transformers==4.30.0

# Web service frameworks
fastapi>=0.109.1
uvicorn==0.17.6
streamlit>=1.30.0

# Utilities
pyyaml>=6.0
psutil>=5.9.0
requests>=2.28.0
